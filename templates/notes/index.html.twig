{% extends 'base.html.twig' %}

{% block title %}Hello CoolStuffController!{% endblock %}

{% block body %}
    <style>
        .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
        .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
    </style>

    <div class="example-wrapper">
        {% for note in notes %}

            <h1>{{ loop.index }}</h1>
            <p> L'étudiant a obtenu la note de : {{ note.note }}.</p>
            <p> Commentaire : {{ note.description }}.</p>
        {% endfor %}
    </div>
{% endblock %}
